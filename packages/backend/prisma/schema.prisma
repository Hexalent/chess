generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl"]
}

datasource db {
  provider = "sqlite"
  url      = "file:dev.db"
}

model Test {
  id Int @id @default(autoincrement())
}
